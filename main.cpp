#include <iostream>
#include <stdlib.h>

using namespace std;

int main()
{
    cout << "***** Convert Strings *****\n" << endl;

    string time;
    int numChars, timeNum;

    cout << "Enter in the time in the following format - H:M:S  ";
    cin >> timeNum;

    numChars = time.length();

    string textTTT = "10";

    timeNum = strtod(textTTT);

    cout << text;

    return 0;
}
